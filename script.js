const textAreaQuestionEl = document.querySelector('.text_area_container');
const keyContainerEl= document.querySelector('key_container');
const btnEl = document.querySelector('button')

// let  text = textAreaQuestion.textContent;

let textEl = textAreaQuestionEl.value;

keyContainerEl.addEventListener('input', () => {
    console.log('1')
})

